/**
 * services.ENV is NOT version controlled
 * services.ENV serves to provide eg a real-world api endpoint base from localhost
 * to use, clone _services.ENV into your dev tree and assign endpoints to properties.
 * import eg servicesENV from config/services.ENV and reference as required.
 */
export default {
    recipesAPIBase: undefined
};
